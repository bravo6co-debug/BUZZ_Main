# ====================================
# BUZZ Platform Environment Variables
# ====================================
# Copy this file to .env and fill in your values

# Supabase Configuration
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
# SERVICE_ROLE_KEY는 Edge Functions에서만 사용 (프론트엔드에서 제거됨)

# Vite Environment Variables (for frontend apps)
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
# VITE_SUPABASE_SERVICE_ROLE_KEY는 더 이상 사용하지 않음 (보안 강화)

# Application Versions
VITE_APP_VERSION=1.0.0
VITE_ADMIN_VERSION=1.0.0
VITE_BIZ_VERSION=1.0.0

# Development Settings
NODE_ENV=development
PORT=3000

# Database (for direct connections if needed)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres

# Optional: Email Settings (if using custom email)
EMAIL_FROM=noreply@yourdomain.com
EMAIL_REPLY_TO=support@yourdomain.com

# Optional: Storage
STORAGE_BUCKET_URL=https://[YOUR-PROJECT].supabase.co/storage/v1

# Security Note:
# NEVER commit the actual .env file to git!
# The .env file should always be in .gitignore
# Only share the ANON_KEY publicly (it's safe for client-side)
# NEVER share the SERVICE_ROLE_KEY publicly (server-side only)